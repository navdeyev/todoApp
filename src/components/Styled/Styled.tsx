import styled from 'styled-components';
import {rem} from './utils';

export const Heading1 = styled.h1`
    margin: 0;
    padding:0;
    font-weight: 700;
    font-size: ${ rem(32) };
    line-height: ${ rem(40) };
`;

